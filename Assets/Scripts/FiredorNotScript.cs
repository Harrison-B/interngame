using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class FiredorNotScript : MonoBehaviour
{

    public Text firedText;

    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        // Text scoreText = GameObject.FindGameObjectWithTag("scoretext").GetComponent<Text>();
        // string scoreToJudge = scoreText.text;


        // int scoreInt;
        // int.TryParse(scoreToJudge, out scoreInt);
        // if (scoreInt < 100)
        // {
        //     //you're fired
        //     firedText.text = "SCORE UNDER 100, YOU ARE FIRED!";
        // }
        // if (scoreInt > 100 && scoreInt < 150)
        // {
        //     //good work
        //     firedText.text = "SCORE ABOVE 100 BUT YOU COULD IMPROVE";
        // } else {
        //     firedText.text = "SCORE ABOVE 150, THE BOSS IS HAPPY :)";
        // }
        //scoreText.text = currentScore.ToString();
    }
}
